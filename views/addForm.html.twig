{% extends 'head.html.twig' %}
 	{% block content %}
		<form action="{{ path('addMedoc') }}" method="post" class="form w40">
		    {{ form_widget(form) }}
		    <input type="submit" name="submit" id="submit" />
		</form>
	{% endblock %}