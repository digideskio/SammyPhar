{% extends 'head.html.twig' %}
 	{% block content %}
		<form action="{{ path('addMedoc') }}" method="post">
		    {{ form_widget(form) }}
		    <input type="submit" name="submit" />
		</form>
	{% endblock %}